import type { CSSProperties, ForwardRefExoticComponent, RefAttributes } from 'react'

export interface BBhyqvGanProps {
  /**
   * Variant
   * Friendly names map to internal IDs:
   *   "Compact Open" → UFteyaTNF
   *   "Compact" → LkCLbYbe1
   *   "Desktop" → djC9hklhA
   */
  variant?: 'Compact Open' | 'Compact' | 'Desktop' | 'djC9hklhA' | 'LkCLbYbe1' | 'UFteyaTNF'
  /**
   * Show Login — pass as `UUQHyrZ5V` not `showLogin`.
   * @default true
   */
  UUQHyrZ5V?: boolean
  onUUQHyrZ5VChange?: string
  style?: CSSProperties
  className?: string
  /** Additional properties */
  [key: string]: unknown
}

declare const BBhyqvGan: ForwardRefExoticComponent<
  BBhyqvGanProps & RefAttributes<HTMLElement>
>
export default BBhyqvGan
