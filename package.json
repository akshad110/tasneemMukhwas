{
  "name": "taskeen-mukhwas",
  "private": true,
  "description": "Monorepo root — installs backend deps on Render when Root Directory is repo root",
  "scripts": {
    "build": "npm --prefix backend install",
    "start": "npm --prefix backend start"
  }
}
